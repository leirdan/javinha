#pragma once
#include "grammar.hpp"
#include "token.hpp"
#include "types.hpp"

using namespace jc;
using namespace jc::grammar;

namespace jc
{
  namespace log
  {
    void debug(const std::string &&msg);
  }
}